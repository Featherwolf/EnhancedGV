# Normalize line endings to LF so the Python backend, shell heredocs in CI,
# and the plugin run correctly on the Linux/SteamOS target regardless of the
# committer's OS.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
